package com.example.serviceCase.repository;

import com.example.serviceCase.entity.RenType;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RentTypeRepository extends JpaRepository<RenType, Integer> {
}
